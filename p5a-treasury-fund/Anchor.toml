[toolchain]

[features]
resolution = true
skip-lint = false

[programs.localnet]
p5a_treasury_fund = "Fund5aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "Localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
